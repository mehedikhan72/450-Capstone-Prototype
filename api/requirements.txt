# ML deps — pinned to the TRIDENT v13 training environment.
# These are read off the artefact itself: joblib.load(bundle)['schema']['env'].
# Unpickling estimators across scikit-learn or LightGBM versions usually does not
# raise; it returns an object that runs and answers slightly differently. The
# engine warns loudly at startup if what is installed differs from what is recorded.
numpy==2.0.2
pandas==2.3.3
scikit-learn==1.6.1
scipy==1.16.3
joblib==1.5.3
pyarrow==18.1.0
lightgbm==4.6.0          # the 'Boosting' member is an LGBMClassifier — required to unpickle
shap==0.50.0             # not used at inference; pinned so the env matches training
threadpoolctl==3.6.0
psutil==7.2.2

# API / job queue deps
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
celery==5.4.0
sqlalchemy==2.0.35
psycopg2-binary==2.9.9
